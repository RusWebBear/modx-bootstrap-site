# Установка HTML-шаблонов для MODX Revolution 2.8.8

## 📦 Содержимое пакета

```
modx-templates/          # HTML-шаблоны страниц
├── home.html           # Главная страница
├── project-detail.html # Детальная страница проекта
├── blog.html           # Блог (создать отдельно)
├── promotions.html     # Акции (создать отдельно)
└── reviews.html        # Отзывы (создать отдельно)

modx-chunks/            # Чанки (повторяющиеся блоки)
├── header.html         # Шапка сайта
├── footer.html         # Подвал сайта
├── projectCard.html    # Карточка проекта
└── reviewCard.html     # Карточка отзыва

modx-assets/            # Статические файлы
├── css/
│   └── custom.css      # Кастомные стили
└── js/
    └── custom.js       # Скрипты интерактивности
```

---

## 🚀 Быстрый старт

### Шаг 1: Подготовка MODX

1. Установите MODX Revolution 2.8.8
2. Войдите в админ-панель
3. Установите необходимые пакеты через "Управление пакетами":
   - **pdoTools** (обязательно!)
   - **FormIt** (для форм)
   - **mFilter2** (для фильтров, опционально)
   - **MIGX** (для галерей, опционально)

### Шаг 2: Загрузка файлов

1. Скопируйте папку `modx-assets` в `/assets/` вашего MODX:
   ```
   /assets/css/custom.css
   /assets/js/custom.js
   ```

2. Проверьте доступность файлов в браузере:
   - `http://ваш-сайт.ru/assets/css/custom.css`
   - `http://ваш-сайт.ru/assets/js/custom.js`

### Шаг 3: Создание шаблонов

1. Перейдите в **Элементы → Шаблоны**
2. Создайте новый шаблон "Главная страница"
3. Скопируйте содержимое `modx-templates/home.html`
4. Сохраните

Повторите для остальных шаблонов:
- `project-detail.html` → Шаблон "Детальная страница проекта"
- И так далее...

### Шаг 4: Создание чанков

1. Перейдите в **Элементы → Чанки**
2. Создайте чанк `header`
3. Скопируйте содержимое `modx-chunks/header.html`
4. Сохраните

Повторите для:
- `footer`
- `projectCard`
- `reviewCard`

### Шаг 5: Создание TV-полей

#### Для проектов (ID родителя: 5)

Перейдите в **Элементы → Template Variables** и создайте:

| Название | Тип | Описание |
|----------|-----|----------|
| `project_area` | Number | Площадь м² |
| `project_price` | Number | Цена в рублях |
| `project_rooms` | Number | Количество комнат |
| `project_type` | Listbox (house=Дом\|\|banya=Баня) | Тип объекта |
| `project_features` | Textarea | Особенности (по одной на строку) |
| `project_image` | Image | Главное изображение |
| `project_specs_floors` | Number | Этажей |
| `project_specs_walls` | Text | Материал стен |
| `project_specs_roof` | Text | Кровля |
| `project_specs_foundation` | Text | Фундамент |
| `project_specs_buildtime` | Text | Срок строительства |

Привяжите все TV к шаблону "Детальная страница проекта".

#### Для отзывов (ID родителя: 20)

| Название | Тип | Описание |
|----------|-----|----------|
| `review_name` | Text | Имя клиента |
| `review_location` | Text | Город/область |
| `review_rating` | Number (1-5) | Оценка |
| `review_project` | Text | Название проекта |
| `review_date` | Date | Дата отзыва |
| `review_verified` | Checkbox | Проверенный отзыв |

### Шаг 6: Создание структуры ресурсов

1. Перейдите в **Ресурсы**
2. Создайте следующую структуру:

```
- Главная (ID: 1) — Шаблон: home.html
  ├── Проекты (ID: 5) — Контейнер
  │   ├── Дом "Северный" (ID: 6) — Шаблон: project-detail.html
  │   ├── Баня "Русская традиция" (ID: 7)
  │   └── ...
  ├── Блог (ID: 10) — Шаблон: blog.html
  ├── Акции (ID: 15) — Шаблон: promotions.html
  └── Отзывы (ID: 20) — Шаблон: reviews.html
```

3. Для каждого ресурса:
   - Заполните `pagetitle` (название)
   - Заполните `description` (мета-описание)
   - Выберите шаблон
   - Заполните TV-поля

### Шаг 7: Настройка главной страницы

1. Откройте ресурс "Главная" (ID: 1)
2. Выберите шаблон "Главная страница"
3. Убедитесь что в контенте указаны правильные ID:
   - `[[~5]]` — ссылка на "Проекты"
   - `[[~10]]` — ссылка на "Блог"
   - `[[~15]]` — ссылка на "Акции"
   - `[[~20]]` — ссылка на "Отзывы"

### Шаг 8: Создание проектов

1. Создайте дочерний ресурс "Дом Северный" (родитель: ID 5)
2. Выберите шаблон "Детальная страница проекта"
3. Заполните TV-поля:
   - `project_area`: 120
   - `project_price`: 3500000
   - `project_rooms`: 4
   - `project_type`: house
   - `project_features`: (по одной на строку)
     ```
     Панорамные окна
     Терраса
     Гараж
     ```
   - `project_image`: загрузите изображение
   - И так далее...

4. Повторите для других проектов

### Шаг 9: Проверка работы

1. Очистите кеш MODX (Управление → Очистить кеш)
2. Откройте сайт в браузере
3. Проверьте:
   - ✅ Главная страница загружается
   - ✅ Отображаются проекты
   - ✅ Работают фильтры
   - ✅ Работает навигация
   - ✅ Мобильное меню работает
   - ✅ Формы отправляются

---

## 🔧 Настройка FormIt (формы обратной связи)

### 1. Настройте email в конфигурации MODX

Перейдите в **Система → Настройки системы**:
- `emailsender` = `info@domabani.ru`
- `emailsubject` = `Заявка с сайта Дома и Бани`

### 2. Проверьте работу формы

Заполните форму на сайте и проверьте почту.

Если письма не приходят:
1. Установите пакет **QuickEmail**
2. Или используйте SMTP (пакет **modExtra → modSMTP**)

---

## 🎨 Кастомизация дизайна

### Изменение цветов

Откройте `/assets/css/custom.css` и измените переменные:

```css
:root {
    --primary-orange: #F97316;  /* Основной цвет */
    --primary-light: #FEC6A1;   /* Светлый акцент */
    --secondary-green: #22C55E; /* Вторичный цвет */
}
```

### Изменение шрифтов

В `custom.css` замените:
```css
body {
    font-family: 'Open Sans', sans-serif;
}
```

---

## 📱 Проверка адаптивности

Откройте сайт в DevTools (F12) и протестируйте:
- iPhone (375px)
- iPad (768px)
- Desktop (1920px)

Все элементы должны корректно отображаться.

---

## 🐛 Решение проблем

### Проекты не отображаются

**Причина:** Неправильный ID родителя в вызове pdoResources

**Решение:**
```php
[[!pdoResources?
  &parents=`5`  <!-- Убедитесь что это ID контейнера "Проекты" -->
  &tpl=`projectCard`
]]
```

### Фильтры не работают

**Причина:** Не загрузился `custom.js`

**Решение:**
1. Проверьте путь к файлу в шаблоне
2. Очистите кеш браузера (Ctrl+Shift+R)
3. Проверьте консоль браузера (F12 → Console)

### Формы не отправляются

**Причина:** Не установлен FormIt

**Решение:**
1. Установите пакет FormIt через "Управление пакетами"
2. Настройте email в конфигурации MODX

### Изображения не загружаются

**Причина:** Неправильный путь к медиа

**Решение:**
1. Загрузите изображения через "Файлы" в админке
2. Используйте TV-поле типа "Image"
3. Путь будет автоматически: `/assets/images/...`

---

## 📊 Рекомендации по SEO

1. **Заполните мета-теги** для каждой страницы:
   - Title (в поле `pagetitle`)
   - Description (в поле `description`)
   - Keywords (создайте TV-поле)

2. **Установите ЧПУ** (уже включено в MODX по умолчанию)

3. **Создайте sitemap.xml**:
   - Установите пакет **GoogleSiteMap**
   - Настройте автогенерацию

4. **Настройте robots.txt**:
   ```
   User-agent: *
   Allow: /
   Sitemap: https://ваш-сайт.ru/sitemap.xml
   ```

---

## 🚀 Оптимизация производительности

1. **Включите кеширование**:
   - Система → Настройки → `cache_resource` = Да

2. **Минифицируйте CSS/JS**:
   - Установите пакет **MinifyX**

3. **Оптимизируйте изображения**:
   - Используйте WebP формат
   - Сжимайте через TinyPNG

4. **Включите Gzip** на сервере (.htaccess):
   ```apache
   <IfModule mod_deflate.c>
     AddOutputFilterByType DEFLATE text/html text/css text/javascript
   </IfModule>
   ```

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи MODX: `/core/cache/logs/error.log`
2. Проверьте документацию: https://docs.modx.com/
3. Задайте вопрос на форуме: https://modx.pro/

---

## ✅ Чек-лист финальной проверки

- [ ] Установлен MODX Revolution 2.8.8
- [ ] Установлены пакеты: pdoTools, FormIt
- [ ] Созданы все шаблоны (home, project-detail)
- [ ] Созданы все чанки (header, footer, projectCard, reviewCard)
- [ ] Созданы все TV-поля
- [ ] Загружены CSS/JS файлы в `/assets/`
- [ ] Создана структура ресурсов
- [ ] Заполнены проекты с TV-полями
- [ ] Заполнены отзывы
- [ ] Работают фильтры проектов
- [ ] Работают формы обратной связи
- [ ] Проверена адаптивность на мобильных
- [ ] Заполнены мета-теги для SEO
- [ ] Очищен кеш MODX
- [ ] Протестирована работа сайта

---

**Поздравляем!** 🎉 Ваш сайт на MODX Revolution 2.8.8 с Bootstrap 5 готов!

---

**Автор:** Юра, poehali.dev  
**Дата:** Январь 2024  
**Версия:** 1.0
